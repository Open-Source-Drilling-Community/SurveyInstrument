apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingsurveyinstrumentservice.fullname" . }}
  labels:
    {{- include "norcedrillingsurveyinstrumentservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingsurveyinstrumentservice.selectorLabels" . | nindent 4 }}
