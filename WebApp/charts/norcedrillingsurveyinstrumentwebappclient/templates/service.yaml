apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingsurveyinstrumentwebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingsurveyinstrumentwebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingsurveyinstrumentwebappclient.selectorLabels" . | nindent 4 }}
